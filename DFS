#include <stdio.h>
int visited[5],a[5][5],n=5;
void dfs(int v){
    printf("%d ",v);
    visited[v]=1;
    for(int i=0;i<n;i++){
        if(a[v][i] && !visited[i])
            dfs(i);
    }
}
int main(){
    dfs(0);
}
